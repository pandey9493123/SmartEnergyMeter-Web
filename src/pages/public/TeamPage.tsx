import TeamSection from '../../components/team/TeamSection';

export default function TeamPage() {
  return (
    <main style={{ flex: 1, backgroundColor: 'var(--surface-page)' }}>
      <TeamSection />
    </main>
  );
}